// Create a function thats prints the multiplication table of a number

function mulitiply(num){
    for(let i=1; i<= num; i++){
        console.log(num*i);
    }
}

mulitiply(10);
mulitiply(18);
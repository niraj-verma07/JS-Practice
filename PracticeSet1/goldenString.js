// A string is a golden string if it starts with the character 'A' or 'a' and hs total length greater than 5

let str = "Apple";

if((str[0] == 'A' || str[0] == 'a') && (str > 5)){
    console.log(`${str} is a golden String`);
}else{
    console.log(`${str} is a golden String`);
}
// Create a number variable num with some value. Now, print "good" id the number is divisible by 10 and "bad" if it is not
let num = 500;
if(num % 10 == 0){
    console.log(`${num} is a good Number`)
}else{
    console.log(`${num} is a bad Number`)
}